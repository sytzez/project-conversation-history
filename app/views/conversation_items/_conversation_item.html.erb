<fieldset id="<%= dom_id conversation_item %>">
  <legend><%= conversation_item.author.email %></legend>

  <%= render conversation_item.item %>

  <small><%= conversation_item.created_at.to_formatted_s(:small) %></small>
</fieldset>
